import React from "react";

const writeHead = () => {
  const question = {
    marginTop: "15px",
    marginBottom: "15px",
  };
  return (
    <div>
      <h2 style={question}>문의하기</h2>
    </div>
  );
};

export default writeHead;
